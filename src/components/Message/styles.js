import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  constainer: {
    marginHorizontal: '15%',
    marginTop: 35,
    marginBottom: 40,
  },
  txtStyle: {
    fontSize: 16,
    lineHeight: 30,
  },
});

export default styles;
